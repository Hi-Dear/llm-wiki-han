# LLM Wiki 운영 규칙 (Schema)

이 볼트는 Obsidian과 Claude Code가 함께 관리하는 **LLM Wiki**다.
(Andrej Karpathy의 LLM Wiki 패턴: 원본과 질의 사이에 LLM이 유지하는 지속적 마크다운 위키를 둔다.)

## 3계층 구조

| 계층         | 위치                        | 규칙                                  |
| ---------- | ------------------------- | ----------------------------------- |
| Raw Source | `Clippings/` + 루트의 사용자 메모 | **절대 수정·삭제 금지.** 읽기 전용 원본           |
| Wiki       | `Wiki/`                   | LLM이 생성·갱신하는 지식 페이지. 유일하게 편집 가능한 영역 |
| Schema     | `CLAUDE.md` (이 파일)        | 운영 규칙. 변경 시 사용자 확인 필요               |

## 위키 페이지 형식

모든 `Wiki/` 페이지는 다음 frontmatter를 갖는다:

```yaml
---
type: wiki
topic: <주제 분류>
sources:
  - "[[원본 파일명]]"
updated: YYYY-MM-DD
---
```

- 본문은 한국어로 작성 (원문이 영어인 학술 자료는 용어 병기)
- 다른 위키 페이지는 `[[위키링크]]`로 연결. 링크를 아끼지 말 것 — 교차참조가 위키의 핵심 가치
- 페이지 하단에 `## 원본` 섹션으로 출처를 명시
- 사실과 해석을 구분: 원본의 주장은 출처와 함께, 위키의 종합·해석은 별도 섹션으로

## 워크플로우

### 1. 수집 (Ingest)
`Clippings/`에 새 파일이 들어오거나 사용자가 "수집해줘"라고 하면:
1. 원본을 읽고 광고·잡음을 걸러 핵심만 추출
2. 기존 위키에 해당 주제 페이지가 있으면 **갱신**, 없으면 생성
3. 관련 페이지들에 교차참조 링크 추가
4. `Wiki/Home.md` 색인 갱신
5. `Wiki/Log.md`에 작업 내역 기록 (아래 로그 형식 참조)

### 2. 질의 (Query)
사용자 질문에는 위키 페이지를 우선 참조해 답한다. 위키에 없는 내용만 원본을 다시 읽고, 그 결과를 위키에 반영한다 (질의가 곧 위키를 성장시킨다).

### 3. 검증 (Verify)
사용자가 "검증해줘"라고 하면:
- 위키 페이지 간 모순 확인
- 깨진 `[[링크]]` 확인
- 시황·뉴스 등 시간에 민감한 페이지의 날짜 확인 후 오래된 정보 표시
- `Home.md` 색인과 실제 파일 일치 확인
- 검증 결과를 `Wiki/Log.md`에 기록

## 로그 형식

모든 작업 후 `Wiki/Log.md` 최상단(날짜 헤더 아래)에 다음 형식으로 추가한다:

```
## YYYY-MM-DD HH:MM

### [작업 유형] 제목
**처리한 원본**: 파일명 또는 질의 내용
**결정 사항**
- 어떤 폴더/페이지를 선택했는지, 왜 그 분류를 골랐는지
- 새 폴더를 만들었다면 그 이유
- 기존 페이지를 갱신했다면 무엇을 추가했는지
- 교차참조를 추가한 이유
```

작업 유형: `수집` / `갱신` / `질의` / `검증`

**Clippings 링크 규칙**: `Clippings/` 폴더 원본을 위키에서 링크할 때는 반드시 `[[Clippings/파일명]]` 형식을 사용한다. 파일명만 쓰면 특수문자(em dash, 스마트 따옴표 등) 인코딩 불일치로 Obsidian이 링크를 찾지 못하고 새 파일을 만든다.

## 금지 사항

- `Clippings/` 및 루트의 사용자 메모(`LLM WIKI.md`, `하네스.md` 등) 수정·삭제 금지
- `.obsidian/` 폴더 수정 금지
- 위키 페이지 삭제는 사용자 확인 후에만
- 원본에 없는 사실을 출처 표기 없이 위키에 쓰지 말 것

## 실패 기록

(에이전트가 실수할 때마다 여기에 한 줄씩 규칙을 추가한다 — 하네스 엔지니어링 원칙)

- `2026-07-14` Clipping 파일명에 U+2013 en dash(`–`)·U+2019 curly apostrophe(`'`) 등 비ASCII 문자가 있으면 편집 도구가 쓰는 ASCII와 달라 Obsidian 링크가 깨짐 → Clippings 파일은 저장 시 파일명을 순수 ASCII로 유지하고, 링크는 `[[Clippings/파일명]]` 경로 포함 형식 사용
